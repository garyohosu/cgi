# SQLite Database Files

This directory contains SQLite database files used by the db.cgi API.

## Security
- Direct access is blocked via .htaccess
- Files can only be accessed through db.cgi API
- Only allowed origins can access the API

## Files
- `*.db` - SQLite database files (ignored by git)
- `.htaccess` - Access protection
